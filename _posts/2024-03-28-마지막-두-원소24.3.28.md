---
layout: post
title: "마지막 두 원소(24.3.28)"
date: 2024-03-28T02:42:14.284Z
tags:
  - 자바
  - 프로그래머스Lv0
---

# 📔문제 설명
![](https://velog.velcdn.com/images/duwnstj12/post/9133925b-c37e-457b-b9fb-97842c6b3652/image.png)
***
# 📄입출력 예시
![](https://velog.velcdn.com/images/duwnstj12/post/ec5a028b-d3b0-4d47-b244-8ea37eef21df/image.png)
***
# ✨문제 풀이 코드
```java
class Solution {
    public int[] solution(int[] num_list) {
        int[] answer = new int[num_list.length+1];
        int last = num_list[num_list.length-1];
        int last2 = num_list[num_list.length-2];
        for(int i=0;i<num_list.length;i++){
            answer[i] = num_list[i];
        }
        if(last>last2){
            answer[num_list.length]=last-last2;
        }else{
            answer[num_list.length]=last*2;
        }
        return answer;
    }
}
```
***
# 😁문제 풀이
1. answer의 배열을 num_list의 길이에 +1을해준다.
이유는 배열뒤에 하나 더 추가되서 배열을 return받아야하기때문이다.
2. last변수에 num_list배열의 마지막 인덱스 번호에 해당하는 값을 넣는다.(인덱스번호는 0부터 시작이기 때문에 -1을 해준다.)
3. last2변수에 num_list배열의 마지막에서 두번째 인덱스 번호에 해당하는 값을 넣는다.(따라서 -2를 해준다.
4. `answer[0<numlist.length]`만큼 반복될수 있게 for문을 만들어준다. 
5. for문을 나와서 if문에 문제에 맞게 조건식을 넣어 문제를 해결했다. 여기서 num_list.length를 인덱스 배열번호에 넣는 이유는 answer 배열길이가 하나가 더 추가가 되었기 때문이다. 