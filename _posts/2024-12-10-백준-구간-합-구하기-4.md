---
layout: post
title: "백준 구간 합 구하기 4"
date: 2024-12-10T09:50:19.189Z
categories:
  - Tech Log
tags:
  - 코딩테스트
---

## 문제 설명
다음은 구간 합을 활용하여 주어진 숫자 배열에서 특정 구간의 합을 빠르게 구하는 문제입니다.
구간 합을 효율적으로 계산하기 위해 **합 배열**을 사용하며 이를 통해 특정 구간의 합을 빠르게 도출할 수 있습니다.

## 문제
![](https://velog.velcdn.com/images/duwnstj12/post/1052dbc5-8d38-4715-877e-71afe06944ad/image.png)

## 알고리즘 개요
합 배열은 배열의 누적합을 저장하여 구간 합을 빠르게 계산할 수 있도록 돕는 기법입니다.

1. **시간 복잡도**
- 합 배열 생성 : $O(N)$
- 구간 합 계산 : $O(1)$
**전체 시간 복잡도** : $O(N+M)$
$N$ : 배열의 길이
$M$ : 질의 횟수

2. **효율성**
합 배열을 사용하면 매번 배열을 순회하지 않고 $i$부터 $j$까지의 합을 빠르게 계산할 수 있습니다.

***

## 풀이 코드
```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class Main {
    /*
     입력
     수의 개수 N , 합을 구해야하는 횟수 M
     N개의 수
     합을 구해야하는 구간 i와 j가 M만큼 있어야함

     출력
     총 M개의 줄에 입력된 i~j수까지의 합을 출력해준다.

      */
    /*
    슈도 코드
    suNo(숫자 개수 ) , quizNo(질의 개수) 저장하기
    for(숫자 개수만큼 반복하기){
    합 배열 생성하기 S[i] = S[i-1]+A[i]
    }
    for(질의 개수만큼 반복하기){
    질의 범위 받기(i~j)
    구간 합 출력하기 S[j]-S[i-1]
    }
     */
    // 최악이 숫자 개수도 10만개, 질의 개수도 10만개이기때문에 Scanner 대신 BufferedReader 사용이 더 좋음
    // 이유는 더 빠르기 때문
    
    public static void main(String[] args) throws IOException {
        BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer stringTokenizer = new StringTokenizer(bufferedReader.readLine());

        //입력 값 suNo, quizNo 생성
        int suNo = Integer.parseInt(stringTokenizer.nextToken());
        int quizNo = Integer.parseInt(stringTokenizer.nextToken());

        //합배열 생성
        long[] s = new long[suNo + 1];

        stringTokenizer = new StringTokenizer(bufferedReader.readLine());

        for (int i = 1; i <= suNo; i++) {
            s[i] = s[i - 1] + Integer.parseInt(stringTokenizer.nextToken());
        }
        for (int q = 0; q < quizNo; q++) {
            stringTokenizer = new StringTokenizer(bufferedReader.readLine());
            int i = Integer.parseInt(stringTokenizer.nextToken());
            int j = Integer.parseInt(stringTokenizer.nextToken());

            System.out.println(s[j] - s[i - 1]);

        }


    }
}
```text
## 알고리즘 설명

### 1. 합 배열 공식
합 배열은 누적합을 계산하여 특정 구간의 합을 빠르게 구하는데 사용됩니다.

- **공식**
$S[i] = S[i-1] + A[i]$
   - $S[i]$ : 합 배열의 $i$번째 값
   - $A[i]$ : 원본 배열의 $i$번째 값
![](https://velog.velcdn.com/images/duwnstj12/post/eb5e296a-010e-4483-ab63-cb6795d05cfa/image.png)

    
- **특징**
    - $S[i]$는 $A[1]$부터 $A[i]$까지의 누적합을 저장합니다.
    - $i$부터 $j$까지의 구간 합은 아래와 같이 계산할 수 있습니다
    $S[j] - S[i-1]$
    
### 2. 구간 합 공식
구간 합은 합 배열을 활용하여 다음과 같이 계산됩니다.

$i$부터 $j$까지의 합 = $S[j] - S[i-1]$

**예시**
배열 $A = [5, 4, 3, 2, 1]$에서 $i=2$, $j=4$인 경우

- **합배열** : $S = [0, 5, 9, 12, 14, 15]$
- $S[4] - S[1] = 14 - 5 = 9$
	결과 : $4 + 3 + 2 = 9$
    
***

## 코드 상세 설명

### 1. 입력 처리
- `BufferedReader`와 `StringTokenizer`를 사용해 입력 값을 효율적으로 읽습니다.
- 첫 줄: 숫자 개수 $N$과 질의 개수 $M$.
- 두 번째 줄: 숫자 배열.

### 2. 합 배열 생성

```java
for (int i = 1; i <= suNo; i++) {
    s[i] = s[i - 1] + Integer.parseInt(stringTokenizer.nextToken());
}

```text
- 합 배열 `s`는 원본 배열의 누적합을 저장합니다.
- $s[i] = s[i-1] + A[i]$

### 3. 구간 합 계산

```java
for (int q = 0; q < quizNo; q++) {
    int i = Integer.parseInt(stringTokenizer.nextToken());
    int j = Integer.parseInt(stringTokenizer.nextToken());
    System.out.println(s[j] - s[i - 1]);
}

```text
- $i$부터 $j$까지의 합은 $s[j] - s[i-1]$로 계산됩니다.
- $O(1)$의 시간 복잡도로 구간 합 계산이 가능합니다.

***

## 시간 복잡도 분석
1.**합 배열 생성** ($O(N)$)
- 입력 배열 $A$의 길이 $N$만큼 순회하며 합 배열 $S$를 계산합니다.
- $N = 100,000$인 경우, 합 배열 생성은 100,000번의 연산을 수행합니다

2.**구간 합 계산** ($O(M)$)
- $M$개의 질의에 대해 합 배열 $S$를 사용해 구간 합을 계산합니다.
- 각 질의에서 $S[j] - S[i-1]$ 연산은 $O(1)$로 처리되므로, $M = 100,000$인 경우, 구간 합 계산은 100,000번의 연산을 수행합니다.

3. **총 연산 횟수**
- $N + M = 100,000 + 100,000 = 200,000$번




